# Copy to `.env` or set in PowerShell.
# DATABASE_URL is required (no default). Example for local SQLite:
DATABASE_URL=sqlite://data/app.db
# Optional bind (defaults shown):
# HOST=0.0.0.0
# PORT=3000
# RUST_LOG=info
